# Oracle DB 설정
# 프로젝트 루트에 .env 파일을 복사하고 실제 값을 입력하세요
# .env 파일은 .gitignore에 포함되어 있어 Git에 커밋되지 않습니다

# Oracle DB API Gateway 엔드포인트 URL
ORACLE_DB_URL=https://f5zsyrqay7emcgtj6ybs474i5q.apigateway.ap-chuncheon-1.oci.customer-oci.com/project/gtdoro

# Oracle DB 스키마 이름 (선택사항, 기본값: gtdoro)
ORACLE_DB_SCHEMA=gtdoro

# OAuth2 인증 정보 (선택사항)
# OAuth2 Client Credentials를 사용하는 경우 설정
# ORACLE_DB_CLIENT_ID=your_client_id_here
# ORACLE_DB_CLIENT_SECRET=your_client_secret_here

# 기본 인증 정보 (하위 호환성, OAuth2가 없을 때 사용)
# ORACLE_DB_USERNAME=your_username
# ORACLE_DB_PASSWORD=your_password
